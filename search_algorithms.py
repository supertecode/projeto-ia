def greedy_search(start, goal, maze):
    # Implementação simplificada de busca gulosa
    return (5,5) #retorna valor qualquer pra não quebrar o código, isso deve ser mudado

def a_star(start, goal, maze):
    # Implementação do algoritmo A*
    return (5,5) #retorna valor qualquer pra não quebrar o código, isso deve ser mudado
